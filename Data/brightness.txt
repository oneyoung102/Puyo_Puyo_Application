uniform sampler2D texture;
uniform float brightness;

void main()
{
    vec4 pixel = texture2D(texture, gl_TexCoord[0].xy);

    vec3 brightColor = pixel.rgb + vec3(brightness);
    
    gl_FragColor = vec4(brightColor, pixel.a) * gl_Color;
}